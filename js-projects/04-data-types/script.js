// String
const firstName = "Sara";

// Number
const age = 30;
const temp = 98.9;

// Boolean

const hasKids = true;

// Null (intentional empty value, typeof null is `object`)

const nullExample = null;

// undefined
let score1;

// intentional undefined
let score2 = undefined;

// Symbol (you wont use this)
const id = Symbol("id");

// BigInt (too big for `number`)
const bigInt = 9007199254740991n;

// Reference Types (any reference type is an `object`, function will have the type `function`)

const numbers = [1, 2, 3, 4];

const objectPerson = {
    name: "Brad",
};

function sayHello() {
    console.log("Hello");
}

// OUTPUT
const output = n;
console.log(output, typeof output);
